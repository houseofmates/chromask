# chromask

a browser extension that makes firefox wear a mask to look like chrome to sites that otherwise won't work.

## what is this?

a browser extension that makes firefox wear a mask to look like chrome to sites that otherwise won't work.

this is a fork of [denschub/chrome-mask](https://github.com/denschub/chrome-mask) with a redesigned ui.

## original vs. this version

this is a fork of the [denschub/chrome-mask](https://github.com/denschub/chrome-mask) project.

### changes from original

- redesigned the ui with a dark pkm aesthetic (varela round font, dark background, yellow accent). all text is now lowercase. extension renamed from chromask to chromask. auto-update checking added. non-english locales removed.

## credits

this project is a fork of [denschub/chrome-mask](https://github.com/denschub/chrome-mask). all credit for the original functionality goes to the original authors.

## license

this project is licensed under the [mates license](license).

## installation

download the `.xpi` file from releases and open it in firefox, or load it temporarily via `about:debugging` > this firefox > load temporary add-on.
